# Railway MongoDB — from your Railway project → MongoDB service → Connect
MONGODB_URL=mongodb://user:password@host.railway.app:port/city-events?authSource=admin

# Railway Redis — from your Railway project → Redis service → Connect
REDIS_URL=redis://default:password@host.railway.app:port

# Database name
MONGODB_DB=city-events
